**crm-import-quality-sql**

A small SQL demo that simulates importing a messy contact list into a CRM: load to staging, normalize, validate, dedupe and produce a simple quality summary.

Run instructions and scoring will be added as the project grows.
